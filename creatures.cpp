#include <iostream>
#include "creatures.hpp"

namespace creatures{

    enum class creature_type;

    special_power(std::string desc, int capacity): desc(std::move(desc)), capacity(capacity){}

    struct creature;

}